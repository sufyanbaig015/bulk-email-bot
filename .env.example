# SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Application Settings
NODE_ENV=development
LOG_LEVEL=info

# Email Settings
FROM_EMAIL=your-email@gmail.com
FROM_NAME=Bulk Email Sender

# High Volume Email Settings (for 10K+ emails/day)
# Uncomment and configure if sending large volumes
# MAX_BULK_EMAILS=10000
# EMAIL_DELAY_MS=50
